name: login_prototype
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl: 0.16.1
  flutter_localizations:
    sdk: flutter

  provider: ^4.3.2+2
  flutter_code_style: ^1.6.8
  cupertino_icons: ^1.0.0
  rxdart: ^0.24.1
  equatable: ^1.2.5
  shared_preferences: ^0.5.12+4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

flutter_intl:
  main_locale: en_GB
  enabled: true

